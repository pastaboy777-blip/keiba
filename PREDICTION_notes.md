# 予想システム 統合メモ（速度指数 × パドック気配）

2つのプロジェクトが1つの予想に合流するための記録。

## 全体像：3つの目
```
総合予想 ＝ 実力軸(スピード指数) ＋ 持続巡航 ＋ 当日の気配(パドック歩様)
```
| 軸 | プロジェクト | 中身 | 信頼度 |
|---|---|---|---|
| 実力軸（タイム) | predict_nankan / speed_index | 過去の実測タイム由来のスピード指数 | 高（検証可能・規律クリーン） |
| 持続巡航 | predict_nankan | 展開・持続力 | 中 |
| 当日の気配 | 本リポジトリ(paddock系) | 歩様・重心貫通・気配スコア | 実験（未検証・加点/裏取り） |

橋渡し = `paddock_predict.py`（指数 × 気配 → 総合ランキング）。

---

## (A) 紙面指数の逆算 → 構造復元
- **正体＝スピード指数（標準タイム=0の相対型)**
- **指数 ≈ −k×(タイム − 基準) ＋ 馬場差**
- **距離係数 k**（距離延長で小＝距離正規化の証拠）:

  | コース | k(点/秒) | 基準タイム(指数0) |
  |---|---|---|
  | 大井1200 | ≈10 | ≈76.0秒 |
  | 大井1400 | ≈9 | ≈90.2秒 |
  | 大井1600 | ≈8 | ≈104.2秒 |

- 検証（page1・大井）: 1200で 76.1→−1／78.0→−22（1.9秒で21点≒11点/秒）、1600で 105.6→−11／108.3→−32（2.7秒で21点≒8点/秒）。距離延長で点/秒が下がる＝距離正規化と一致。
- **馬場差は隠れ変数**で紙面出力だけからは日別に完全復元不可 → 「構造＋係数」までが(A)の到達点。

## (B) 自前スピード指数エンジン
- 場所: `predict_nankan` の `scripts/speed_index.py`（branch: claude/handoff-notes-review-swyctx）
- **馬場差を「その日の距離別・勝ちタイム − 基準」で自前観測**（＝紙面より透明。隠れ変数を観測化）
- 実行: `python3 scripts/speed_index.py --date 2026-07-20 --place 大井`
- 検証: 7/20大井で動作確認。紙面値(76.1→−1)と一致（-10×(76.1−76.0)=−1）。
- 位置づけ: タイムベースの相対指数＝実力軸の第3の目。軸は不変、指数は加点・裏取りの参考。

## 本リポジトリ側（パドック気配）
- `paddock_gait.py` 歩様数値化 / `skeleton_overlay.py` 骨格・背骨・重心・重心貫通 /
  `paddock_compare.py` 気配スコア / `paddock_ledger.py` 気配台帳（時系列) /
  `speed_index.py` 指数式の逆算ツール(汎用) / `paddock_predict.py` 指数×気配の合成。

---

## 次にやるべき：検証（新機能より優先）
- **スピード指数**: アウトオブサンプルで紙面値との一致率／実着順との相関。
- **気配スコア**: 「気配上位が好走したか」を数十レースで的中率検証（`paddock_ledger.py` に蓄積）。
- **重み**: 実力:気配の配分を結果データで最適化（現状 6:4 は仮）。
- **基準タイム再calibrate**: 大井の通年・良馬場勝ちタイムで。他距離(1500/1700/2000)の基準確定。

## 正直な位置づけ
- 土台（速度指数＋展開)は堅く実戦投入可。
- パドック気配は将来性のある実験で、**過信せず検証データを貯めて重みを育てる**段階。
- 両方を正直に扱う現設計は、過信していない点で健全。

# インフォグラフ雛形（X/note用）

前セッションで作った「変態か、変態以外か。」ブランドのインフォグラフ雛形。
scratchpadは毎セッション消えるので、再利用する雛形はここに保存する（引き継ぎメモの注記に対応）。

## ファイル
- `render.py` … HTML→PNGレンダラ。`python3 render.py input.html output.png [width]`
  - Playwright chromium `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`、device_scale_factor=2、フォント WenQuanYi Zen Hei。
  - 事前に `pip install playwright`（ブラウザは既存を使うので `playwright install` は不要）。
- `tegen_daymodel_tekijikan.html` … **鉄則16の時間帯スライド作戦**（前半41→後半40）の作戦ボード。
  レース番号→勝ち圏上がりの棒グラフ + 前半/後半の狙い方タイル + 実証コメント。
- `tegen_lap_kaibou.html` … **ラップ解剖**（実ラップ棒グラフ + 位置取り二層化 前40.9/後399 + 勝ち圏上がりの読み替え）。

## 配色テンプレ
背景 `#0f1f33` / アクセント 橙`#ff9e64`・黄`#ffd54a`・水`#5cc8e0`・緑`#7ee0a0`・警戒`#ff8a8a`。幅1120px。
上部にブランド帯、フッター右に **By Claude AI**。編集フィードバックは1項目ずつ来るので都度直して再送。

## 使い方
1. 対象レースのHTMLを雛形からコピーして数値・馬名を差し替え。
2. `python3 render.py <html> <png> 1120` でPNG化。
3. `SendUserFile` で送付。

## zubu_blue_card.html ★本人確認済みの標準フォーマット（2026-07-29）
「AIズブ穴」カードの本命フォーマット。**画像を頼まれたら基本これ**。
- 青サンバースト背景（`repeating-conic-gradient` のレイ）＋白行カード＋金枠。
- 構成：ブランド帯 → 大見出し `日付 場 R範囲` ＋ 金バッジ`ズブ穴` ＋ 紺バッジ`○○版`
  → リード文 → 列見出し `R / ◎本命 / ○相手 / メモ` → 各レース1行 → 🔑本線の一撃バー → By Claude AI。
- 列幅：R=104 / ◎=386 / ○=346 / メモ=残り。`.memo` は **flex 子要素にしない**
  （`display:flex` を入れると日本語が1文字ずつ縦に潰れる。`align-self:center` を使う）。
- **メモの分量＝3行・90〜110字**（本人が見本を提示して確認 2026-07-29「このくらいで良いよ」）。
  長短で二度往復した末に確定した基準。**この行数を超えたら削る**。
  中身は ◎の根拠(脚質・馬体重・実績の数字) → ○との比較 → 減点材料 → `ヒモ⑥②` の順。
- **調教は前面に出さない**（本人指摘 2026-07-29／鉄則7）。追切時計・短評をメモに並べない。
  必要なら「状態は上向き」「仕上げの裏付けは○が上」程度の一言に圧縮する。バッジも調教推しにしない。
- 見送り推奨レースは `class="row skip"`（グレー行）にして本線と視覚的に区別する。
- 強調色：`b`=紺 / `.r`=赤(減点・警戒) / `.g`=緑(運用指示)。本命の一撃には 🔥。
- レンダリング：`python3 render.py zubu_blue_card.html out.png 1480`

## トリプル馬単の金額計算（重要）
**1点＝50円**（100円ではない）。買い目の点数×50円で表記する。
- 絞り 4×4×4 = 64点 → **3,200円**
- 本線 6×6×6 = 216点 → **10,800円**
- 総取り 8×8×8 = 512点 → **25,600円**
本人指摘 2026-07-30。100円で計算して差し戻された。

## 公開用カードの用語ルール（本人指示 2026-07-30）
内部の指標名を**そのまま公開カードに書かない**。読み手には結果と根拠だけを見せる。
| 内部の呼び名 | 公開カードでの言い方 |
|---|---|
| 踏ん張り／踏ん張り指数 | 「上がりが落ちない馬」「かかった時に残した実績」「該当N回」 |
| 399 | 「下地」 |
| 確定版・○○一本 | 使わない（タイトルは素直に「トリプル馬単 10R→11R→12R」） |

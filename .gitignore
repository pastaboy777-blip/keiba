__pycache__/
*.py[cod]
.venv/
venv/
.env

# スクレイピングのキャッシュ・収集データ
data/cache/
data/*.parquet
data/*.csv
data/*.sqlite
data/*.db

# 生成物(新聞HTML など)
out/
# ただし予想ログだけは残す。
# ⚠️ このログは「発走前にこう予想した」という記録そのもの。コンテナは使い捨てなので
#    コミットしないと消える。走る前にコミットしておくこと（コミット時刻が証拠になる）。
!out/jra_ana_log.csv
!out/jra_ana_*.csv

.DS_Store
.idea/
.vscode/

# keibabook cookie(認証情報・コミット禁止)
data/.keibabook_cookie
.keibabook_cookie
*.cookie

# 学習済みモデル(生成物)
data/spark_model.json

# 大系統測定(生成物・既定値はped_stats.pyに埋め込み)
data/pedigree_stats.json

# sarabure cookie(認証情報・コミット禁止)
data/.sarabure_cookie

# CLAUDE.md — 作業ルール・運用メモ

このリポジトリは南関競馬（大井ほか）の予想新聞・指数システム。
以下はユーザーとの取り決め（恒久ルール）。作業時は必ず守ること。

## 恒久ルール

1. **結果が出たら、必ずラップ（ペース）分析もする。**（2026-07-23 合意）
   - レース結果を照合するときは、着順の当たり外れだけでなく
     `src/nankeiba/core/lap.py` で**ペース分析**を必ず添える。
   - 出すもの：勝ちタイム・平均ハロン・前半/上がりバランス・
     **ペース判定 H/M/S**・**決着傾向（前残り/差し）**・**上がり3F上位馬**。
   - 目的：①予想の「展開読み」が当たっていたかの答え合わせ、
     ②上がり最速馬など**次走の狙い目**を拾う。
   - データ源：楽天 race_performance の「走破タイム＋推定上がり3F」
     （区間ラップは無いので、前半＝走破タイム−上がり3F で近似）。

2. **開発は必ず指定ブランチ**：`claude/newspaper-index-forecast-oin5av`。

3. **指数はマキシマム新聞由来（Engine B）が本体**。TARGET／競馬の天才は
   血統・展開・スマート出馬表など「考え方」を補助的に使う。詳細は
   `docs/keiba_techniques.md`。

4. **正直な検証を徹底**：当たった/外れたを盛らずに記録する。
   指数は「複勝軸」として強い（実測 複勝率67%）が、1着固定はブレる。
   混戦（①②指数差が小さい）レースは頭固定しない。

5. **過去の開催で検証しない。**（2026-08-02 指定・1回しか言わないから覚えて）
   > 「過去の開催でどうだったか検証しないで良いです」
   - 全期間バックテスト、過去開催をまとめた回収率・勝率の集計は**やらない**。
     「n万頭で測ったら効かなかった」を持ち出さない。
   - 見るのは**目の前の開催**。その日のレースを、その日の材料で説明する。
   - ルール4（正直な記録）とは別物。**結果の記録はする／過去開催の一括検証はしない**。
   - 頼まれていないのに `--backtest` 系や全期間スキャンを回さないこと。

## よく使うコマンド
- テスト：`python3 -m unittest discover -s tests -q`
- 新聞生成：`python3 scripts/fetch_rakuten.py --date YYYYMMDD --place 大井 --race N`
- 全12R：`python3 scripts/make_paper_all.py --date YYYYMMDD --place 大井`
